# Blumark24 Talent

واجهة React + Vite + Tailwind عربية RTL لمنصة مواهب وكاستنج، مع تحويل أزرار التواصل إلى واتساب.

## التشغيل محلياً

```bash
npm install
npm run dev
```

## بناء نسخة الإنتاج

```bash
npm run build
```

## الرفع على GitHub

```bash
git init
git add .
git commit -m "Initial Blumark24 Talent landing page"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

## ملاحظات

- رقم الواتساب مضبوط داخل `src/App.jsx`:
  `966533116166`
- التصميم مضبوط RTL من ملف `index.html` وداخل مكوّن `App`.
- لا توجد باقات أو Pricing في هذه النسخة.
